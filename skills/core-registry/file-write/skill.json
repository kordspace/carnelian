{
  "name": "file-write",
  "description": "Write content to a file on the local filesystem (owner approval required)",
  "runtime": "native",
  "version": "1.0.0",
  "capabilities_required": ["fs.write"],
  "openclaw_compat": {
    "emoji": "📝",
    "tags": ["filesystem", "write", "native"]
  }
}
