[package]
name = "yaml-parse"
version = "0.1.0"
edition = "2021"

[dependencies]
serde_json = "1.0"
serde_yaml = "0.9"
serde = { version = "1.0", features = ["derive"] }

[profile.release]
opt-level = 3
lto = true
