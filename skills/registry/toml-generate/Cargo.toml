[package]
name = "toml-generate"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "toml-generate"
path = "src/main.rs"

[dependencies]
toml = "0.8"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
