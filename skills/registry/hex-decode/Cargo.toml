[package]
name = "hex-decode"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "hex-decode"
path = "src/main.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
hex = "0.4"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
