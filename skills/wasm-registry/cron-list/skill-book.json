{
  "name": "cron-list",
  "version": "1.0.0",
  "runtime": "node",
  "entrypoint": "index.ts",
  "description": "List all cron jobs with optional filtering for disabled jobs",
  "parameters": {
    "type": "object",
    "properties": {
      "includeDisabled": {
        "type": "boolean",
        "default": false,
        "description": "Include disabled jobs in the results"
      }
    }
  },
  "output": {
    "type": "object",
    "properties": {
      "count": {
        "type": "number",
        "description": "Total number of jobs returned"
      },
      "jobs": {
        "type": "array",
        "description": "Array of cron job objects with schedules and payloads"
      }
    }
  }
}
