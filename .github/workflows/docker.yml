name: Docker Release

on:
  push:
    branches: [main, development]
    tags:
      - 'v*'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  # Build AMD64 image
  build-amd64:
    name: Build AMD64
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha

      - name: Build and push AMD64 image
        uses: docker/build-push-action@v5
        with:
          context: .
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}-amd64
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # Build ARM64 image - DISABLED: Takes 6+ hours via QEMU emulation
  # To enable ARM64 builds:
  # 1. Use GitHub Large Runners with native ARM64 support (paid)
  # 2. Use self-hosted ARM64 runners
  # 3. Use cross-compilation (implemented below but commented out)
  # 
  # For now, AMD64 image is sufficient for most deployments.
  # ARM64 users can build locally with: docker build --platform linux/arm64 .
  #
  # build-arm64:
  #   name: Build ARM64
  #   runs-on: ubuntu-latest
  #   timeout-minutes: 120  # 2 hour timeout
  #   permissions:
  #     contents: read
  #     packages: write
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #
  #     - name: Install cross-compilation tools
  #       run: |
  #         sudo apt-get update
  #         sudo apt-get install -y gcc-aarch64-linux-gnu g++-aarch64-linux-gnu
  #
  #     - name: Setup Rust
  #       uses: dtolnay/rust-toolchain@stable
  #       with:
  #         targets: aarch64-unknown-linux-gnu
  #
  #     - name: Cache Cargo dependencies
  #       uses: Swatinem/rust-cache@v2
  #       with:
  #         shared-key: carnelian-rust-arm64
  #         key: arm64-cross
  #
  #     - name: Install cargo-zigbuild for cross-compilation
  #       run: cargo install cargo-zigbuild
  #
  #     - name: Cross-compile for ARM64
  #       env:
  #         SQLX_OFFLINE: true
  #         CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: aarch64-linux-gnu-gcc
  #       run: |
  #         cargo zigbuild --release --target aarch64-unknown-linux-gnu --bin carnelian
  #         mkdir -p target/docker-arm64
  #         cp target/aarch64-unknown-linux-gnu/release/carnelian target/docker-arm64/
  #
  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v3
  #
  #     - name: Log in to Container Registry
  #       uses: docker/login-action@v3
  #       with:
  #         registry: ${{ env.REGISTRY }}
  #         username: ${{ github.actor }}
  #         password: ${{ secrets.GITHUB_TOKEN }}
  #
  #     - name: Extract metadata
  #       id: meta
  #       uses: docker/metadata-action@v5
  #       with:
  #         images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
  #         tags: |
  #           type=ref,event=branch
  #           type=ref,event=pr
  #           type=semver,pattern={{version}}
  #           type=semver,pattern={{major}}.{{minor}}
  #           type=sha
  #
  #     - name: Build and push ARM64 image (pre-compiled binary)
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: .
  #         file: Dockerfile.prebuilt
  #         platforms: linux/arm64
  #         push: true
  #         tags: ${{ steps.meta.outputs.tags }}-arm64
  #         labels: ${{ steps.meta.outputs.labels }}
  #         cache-from: type=gha,scope=arm64
  #         cache-to: type=gha,mode=max,scope=arm64
  #         build-args: |
  #           TARGETARCH=arm64

  # Create multi-arch manifest - DISABLED: ARM64 build disabled
  # Uncomment when ARM64 builds are re-enabled
  #
  # create-manifest:
  #   name: Create Manifest
  #   runs-on: ubuntu-latest
  #   needs: [build-amd64, build-arm64]
  #   permissions:
  #     contents: read
  #     packages: write
  #   steps:
  #     - name: Log in to Container Registry
  #       uses: docker/login-action@v3
  #       with:
  #         registry: ${{ env.REGISTRY }}
  #         username: ${{ github.actor }}
  #         password: ${{ secrets.GITHUB_TOKEN }}
  #
  #     - name: Extract metadata
  #       id: meta
  #       uses: docker/metadata-action@v5
  #       with:
  #         images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
  #         tags: |
  #           type=ref,event=branch
  #           type=ref,event=pr
  #           type=semver,pattern={{version}}
  #           type=semver,pattern={{major}}.{{minor}}
  #           type=sha
  #
  #     - name: Create and push manifest
  #       run: |
  #         for tag in ${{ steps.meta.outputs.tags }}; do
  #           docker manifest create $tag \
  #             ${tag}-amd64 \
  #             ${tag}-arm64
  #           docker manifest push $tag
  #         done
